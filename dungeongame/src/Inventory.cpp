#include "Inventory.h"

// Implementation notes (for Week 03 - Templates & Linked Lists):
// - Inventory is now implemented as a thin wrapper around LinkedList<Item>
// - All implementation is in the header file since it uses templates
// - This demonstrates how template-based containers can simplify implementations

namespace dungeongame {

// Inventory implementation is now entirely in the header file
// since it uses the LinkedList template class

} // namespace dungeongame
