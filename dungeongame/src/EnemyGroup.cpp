#include "EnemyGroup.h"

// Implementation note (Week 02): this translation unit is intentionally
// minimal because the ADT's operations are provided inline via the header
// and rely on `vector`. The educational goal is to focus on the ADT
// interface and how the standard library can be used as an implementation.

namespace dungeongame {

// No extra implementation required; header uses inline methods

} // namespace dungeongame
