# Week 11 Worksheet — Sorting & Search (Game Integrated)

1) Run the array benchmarks
- From the game main menu choose 27 (Array Sort Benchmarks).
- Capture the CSV output (redirect the game output to a file or copy/paste the CSV lines):

  size,algorithm,time_us,comparisons

- Paste into Excel/Sheets and make a log-log plot of time_us vs size for each algorithm.

2) Analyze sample results
- Which algorithms show O(n²) growth? Which show O(n log n)?
- For the largest tested sizes, which algorithms are practical for one-off sorts? Which are not?

3) Run Inventory sorts
- From the game main menu choose 28 (Run all sorts on Inventory).
- Observe comparisons and times printed for each algorithm. Note any differences in ordering.

4) Short answers (brief):
- Why does binary search require sorted data? (2-3 sentences)
- For a small list (n < 20), is insertion sort ever a reasonable choice? Why?
- How does extra memory use differ between merge sort and quick sort?

5) Submission
- Upload your plots and a short write-up (1 page) answering the questions above.
- If you modified any code, include the changed files and a brief note describing your change.
